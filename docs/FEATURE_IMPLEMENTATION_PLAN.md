# ポートフォリオサイト 機能追加実装計画書

**作成日**: 2026-01-19
**プロジェクト**: myportfoliosite
**対象**: Next.js 15 + NestJS 11 モノレポ構成

---

## 目次

1. [現状分析](#現状分析)
2. [機能一覧と優先度](#機能一覧と優先度)
3. [実装ロードマップ](#実装ロードマップ)
4. [各機能の詳細仕様](#各機能の詳細仕様)
5. [技術的考慮事項](#技術的考慮事項)
6. [リスクと対策](#リスクと対策)

---

## 現状分析

### 実装済み機能

| 機能 | 状態 | 備考 |
|------|------|------|
| ホームページ | ✅ 完了 | Hero, GitHub/Qiitaプレビュー |
| ポートフォリオページ | ✅ 完了 | リポジトリ一覧、コントリビューション |
| アバウトページ | ✅ 完了 | 自己紹介、職歴 |
| 記事ページ | ✅ 完了 | Qiita記事一覧 |
| お問い合わせ | ✅ 完了 | reCAPTCHA付きフォーム |
| ダークモード | ✅ 完了 | next-themes + 切り替えUI |
| レスポンシブ | ✅ 完了 | モバイル対応済み |
| SEO | ✅ 完了 | メタデータ、構造化データ |

### 技術スタック

```text
Frontend: Next.js 15 (App Router), React 19, TypeScript, styled-components
Backend:  NestJS 11, Pino Logger, Resend (メール)
Testing:  Vitest, Playwright, Jest
Infra:    Turborepo (モノレポ), pnpm
```

---

## 機能一覧と優先度

### 優先度マトリクス

| 機能 | 効果 | 難易度 | 工数 | 優先度 |
|------|:----:|:------:|:----:|:------:|
| 1. スキルセクション | 高 | 低-中 | 4-6h | ⭐⭐⭐⭐⭐ |
| 2. プロジェクト詳細ページ | 高 | 中 | 8-12h | ⭐⭐⭐⭐⭐ |
| 3. ページ遷移アニメーション | 中 | 低 | 2-4h | ⭐⭐⭐⭐ |
| 4. 閲覧分析（Analytics） | 中 | 低 | 1-2h | ⭐⭐⭐⭐ |
| 5. 多言語対応（i18n） | 高 | 中 | 8-12h | ⭐⭐⭐ |
| 6. ブログ機能 | 高 | 高 | 16-24h | ⭐⭐⭐ |
| 7. お問い合わせ履歴永続化 | 中 | 高 | 12-20h | ⭐⭐ |

---

## 実装ロードマップ

### Phase 1: クイックウィン（1-2日）

```
┌─────────────────────────────────────────────────────────────┐
│  Day 1-2: 即効性の高い機能                                    │
├─────────────────────────────────────────────────────────────┤
│  [x] ダークモードUI (実装済み)                                │
│  [ ] 閲覧分析 (Vercel Analytics)                            │
│  [ ] ページ遷移アニメーション (framer-motion)                 │
└─────────────────────────────────────────────────────────────┘
```

### Phase 2: 価値向上（3-5日）

```
┌─────────────────────────────────────────────────────────────┐
│  Day 3-7: ポートフォリオとしての価値向上                       │
├─────────────────────────────────────────────────────────────┤
│  [ ] スキルセクション                                        │
│  [ ] プロジェクト詳細ページ                                   │
└─────────────────────────────────────────────────────────────┘
```

### Phase 3: グローバル対応（1週間）

```
┌─────────────────────────────────────────────────────────────┐
│  Week 2: 国際化とコンテンツ強化                               │
├─────────────────────────────────────────────────────────────┤
│  [ ] 多言語対応 (next-intl)                                  │
│  [ ] ブログ機能 (MDX)                                        │
└─────────────────────────────────────────────────────────────┘
```

### Phase 4: エンタープライズ（オプション）

```
┌─────────────────────────────────────────────────────────────┐
│  Week 3+: 高度な機能（必要に応じて）                          │
├─────────────────────────────────────────────────────────────┤
│  [ ] お問い合わせ履歴永続化                                   │
│  [ ] 管理画面                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 各機能の詳細仕様

---

### 1. スキルセクション

**目的**: 技術スタックを視覚的に表示し、採用担当者への訴求力を向上

#### 要件定義

| 項目 | 内容 |
|------|------|
| 配置場所 | Aboutページ or 新規 `/skills` ページ |
| データソース | 静的定義 + GitHub言語データ |
| 表示形式 | カテゴリ別カード + 習熟度バー |

#### UI設計

```
┌─────────────────────────────────────────────────────────────┐
│  Skills                                                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [Frontend]              [Backend]            [DevOps]      │
│  ┌─────────────┐        ┌─────────────┐      ┌──────────┐  │
│  │ React  ████░│        │ Node.js ███░│      │ Docker ██│  │
│  │ Next.js████░│        │ NestJS ████░│      │ AWS   ███│  │
│  │ TypeScript██│        │ Python  ██░░│      │ GCP    ██│  │
│  │ CSS    ████░│        │ Go      █░░░│      │ CI/CD ███│  │
│  └─────────────┘        └─────────────┘      └──────────┘  │
│                                                             │
│  [Languages from GitHub]                                    │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ TypeScript 45% │ Python 25% │ Go 15% │ Other 15%     │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 実装計画

**新規ファイル**:
```
apps/web/
├── components/
│   └── features/
│       └── Skills/
│           ├── Skills.tsx           # メインコンポーネント
│           ├── SkillCard.tsx        # スキルカード
│           ├── SkillBar.tsx         # 習熟度バー
│           ├── SkillCategory.tsx    # カテゴリコンテナ
│           ├── types.ts             # 型定義
│           └── index.ts             # エクスポート
├── data/
│   └── skills.ts                    # スキルデータ定義
└── app/
    └── about/
        └── page.tsx                 # スキルセクション追加
```

**データ構造**:
```typescript
// apps/web/data/skills.ts
export type SkillLevel = 1 | 2 | 3 | 4 | 5;

export interface Skill {
  name: string;
  level: SkillLevel;
  icon?: string;
  color?: string;
}

export interface SkillCategory {
  name: string;
  icon: string;
  skills: Skill[];
}

export const skillsData: SkillCategory[] = [
  {
    name: "Frontend",
    icon: "🎨",
    skills: [
      { name: "React", level: 5, color: "#61DAFB" },
      { name: "Next.js", level: 5, color: "#000000" },
      { name: "TypeScript", level: 4, color: "#3178C6" },
      // ...
    ],
  },
  // Backend, DevOps, etc.
];
```

**実装ステップ**:
1. [ ] スキルデータファイル作成 (30min)
2. [ ] 型定義作成 (15min)
3. [ ] SkillBar コンポーネント (45min)
4. [ ] SkillCard コンポーネント (45min)
5. [ ] SkillCategory コンポーネント (30min)
6. [ ] Skills メインコンポーネント (45min)
7. [ ] Aboutページへの統合 (30min)
8. [ ] レスポンシブ対応 (30min)
9. [ ] テスト作成 (45min)

**依存パッケージ**: なし（styled-componentsで実装）

---

### 2. プロジェクト詳細ページ

**目的**: 各プロジェクトの詳細情報を表示し、技術的な深さをアピール

#### 要件定義

| 項目 | 内容 |
|------|------|
| ルート | `/portfolio/[slug]` |
| データソース | MDX + frontmatter or JSON |
| コンテンツ | 概要、技術スタック、課題と解決策、スクリーンショット |

#### UI設計

```
┌─────────────────────────────────────────────────────────────┐
│  ← Back to Portfolio                                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [Screenshot/Demo Image]                                    │
│  ┌───────────────────────────────────────────────────────┐ │
│  │                                                       │ │
│  │                    Project Image                      │ │
│  │                                                       │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  Project Title                                              │
│  ─────────────                                              │
│  Short description of the project...                        │
│                                                             │
│  [GitHub] [Live Demo]                                       │
│                                                             │
│  Tech Stack                                                 │
│  ──────────                                                 │
│  [React] [TypeScript] [Node.js] [PostgreSQL]               │
│                                                             │
│  Overview                                                   │
│  ────────                                                   │
│  Detailed project description...                            │
│                                                             │
│  Challenges & Solutions                                     │
│  ──────────────────────                                     │
│  • Challenge 1: ...                                         │
│    Solution: ...                                            │
│                                                             │
│  Key Learnings                                              │
│  ─────────────                                              │
│  • Learning point 1                                         │
│  • Learning point 2                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 実装計画

### アプローチA: JSON + 静的生成（推奨

```
apps/web/
├── data/
│   └── projects/
│       ├── index.ts              # プロジェクト一覧エクスポート
│       └── projects.json         # プロジェクトデータ
├── app/
│   └── portfolio/
│       ├── page.tsx              # 一覧ページ（既存）
│       └── [slug]/
│           ├── page.tsx          # 詳細ページ
│           └── head.tsx          # メタデータ
├── components/
│   └── features/
│       └── ProjectDetail/
│           ├── ProjectDetail.tsx
│           ├── ProjectHeader.tsx
│           ├── TechStack.tsx
│           ├── ChallengeSection.tsx
│           ├── ImageGallery.tsx
│           └── index.ts
└── public/
    └── projects/
        └── [slug]/
            └── screenshot.png    # プロジェクト画像
```

**データ構造**:
```typescript
// apps/web/data/projects/types.ts
export interface Project {
  slug: string;
  title: string;
  description: string;
  longDescription: string;
  thumbnail: string;
  images: string[];
  techStack: string[];
  githubUrl?: string;
  liveUrl?: string;
  challenges: {
    challenge: string;
    solution: string;
  }[];
  learnings: string[];
  featured: boolean;
  publishedAt: string;
}
```

**実装ステップ**:
1. [ ] プロジェクトデータ構造設計 (30min)
2. [ ] サンプルプロジェクトデータ作成 (1h)
3. [ ] ProjectDetail コンポーネント群作成 (2h)
4. [ ] 動的ルート `[slug]/page.tsx` 作成 (1h)
5. [ ] `generateStaticParams` で静的生成 (30min)
6. [ ] ポートフォリオ一覧からのリンク追加 (30min)
7. [ ] 画像最適化（next/image） (30min)
8. [ ] レスポンシブ対応 (45min)
9. [ ] テスト作成 (1h)

**依存パッケージ**: なし

---

### 3. ページ遷移アニメーション

**目的**: 滑らかな遷移でUXを向上

#### 要件定義

| 項目 | 内容 |
|------|------|
| ライブラリ | framer-motion |
| アニメーション | フェード + スライド |
| 対象 | ページ全体、セクション |

#### 実装計画

**新規ファイル**:
```
apps/web/
├── components/
│   └── animations/
│       ├── PageTransition.tsx    # ページ遷移ラッパー
│       ├── FadeIn.tsx            # フェードインコンポーネント
│       ├── SlideIn.tsx           # スライドインコンポーネント
│       └── index.ts
└── app/
    └── template.tsx              # ページ遷移用テンプレート
```

**実装例**:
```typescript
// apps/web/components/animations/PageTransition.tsx
"use client";

import { motion, AnimatePresence } from "framer-motion";
import { usePathname } from "next/navigation";

const pageVariants = {
  initial: { opacity: 0, y: 20 },
  enter: { opacity: 1, y: 0 },
  exit: { opacity: 0, y: -20 },
};

export function PageTransition({ children }: { children: React.ReactNode }) {
  const pathname = usePathname();

  return (
    <AnimatePresence mode="wait">
      <motion.div
        key={pathname}
        initial="initial"
        animate="enter"
        exit="exit"
        variants={pageVariants}
        transition={{ duration: 0.3, ease: "easeInOut" }}
      >
        {children}
      </motion.div>
    </AnimatePresence>
  );
}
```

**実装ステップ**:
1. [ ] framer-motion インストール (5min)
2. [ ] PageTransition コンポーネント作成 (30min)
3. [ ] FadeIn/SlideIn ユーティリティ作成 (30min)
4. [ ] template.tsx でページ遷移適用 (15min)
5. [ ] 各セクションにアニメーション追加 (1h)
6. [ ] パフォーマンス確認 (30min)

**依存パッケージ**:
```bash
pnpm add framer-motion --filter web
```

---

### 4. 閲覧分析（Analytics）

**目的**: 訪問者の行動を把握し、改善に活用

#### 要件定義

| 項目 | 内容 |
|------|------|
| サービス | Vercel Analytics（推奨）or Plausible |
| 取得データ | PV、滞在時間、流入元、デバイス |
| プライバシー | Cookie不要、GDPR対応 |

#### 実装計画（Vercel Analytics）

**変更ファイル**:
```
apps/web/
├── package.json              # @vercel/analytics 追加
└── app/
    └── layout.tsx            # Analytics コンポーネント追加
```

**実装例**:
```typescript
// apps/web/app/layout.tsx
import { Analytics } from "@vercel/analytics/react";
import { SpeedInsights } from "@vercel/speed-insights/next";

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        <Analytics />
        <SpeedInsights />
      </body>
    </html>
  );
}
```

**実装ステップ**:
1. [ ] @vercel/analytics インストール (5min)
2. [ ] layout.tsx に追加 (10min)
3. [ ] Vercel ダッシュボードで確認 (15min)
4. [ ] カスタムイベント追加（オプション） (30min)

**依存パッケージ**:
```bash
pnpm add @vercel/analytics @vercel/speed-insights --filter web
```

---

### 5. 多言語対応（i18n）

**目的**: グローバル企業への訴求、海外エンジニアとの交流

#### 要件定義

| 項目 | 内容 |
|------|------|
| ライブラリ | next-intl |
| 対応言語 | 日本語（デフォルト）、英語 |
| URL構造 | `/ja/...`, `/en/...` |
| 切り替え | ヘッダーに言語セレクタ |

#### 実装計画

**ファイル構造**:
```
apps/web/
├── messages/
│   ├── ja.json               # 日本語翻訳
│   └── en.json               # 英語翻訳
├── i18n/
│   ├── config.ts             # i18n設定
│   ├── request.ts            # リクエストハンドラ
│   └── routing.ts            # ルーティング設定
├── app/
│   └── [locale]/             # ロケールベースルーティング
│       ├── layout.tsx
│       ├── page.tsx
│       ├── about/
│       ├── portfolio/
│       ├── article/
│       └── contact/
├── components/
│   └── navigation/
│       └── LanguageSwitcher.tsx
└── middleware.ts             # ロケール検出
```

**翻訳ファイル例**:
```json
// messages/ja.json
{
  "navigation": {
    "home": "ホーム",
    "about": "自己紹介",
    "portfolio": "ポートフォリオ",
    "article": "記事",
    "contact": "お問い合わせ"
  },
  "hero": {
    "greeting": "こんにちは、",
    "name": "Mirrorman",
    "role": "フルスタックエンジニア",
    "description": "モダンなWebアプリケーションを構築しています"
  }
  // ...
}
```

**実装ステップ**:
1. [ ] next-intl インストール (10min)
2. [ ] i18n設定ファイル作成 (30min)
3. [ ] 翻訳ファイル作成（日本語） (1h)
4. [ ] 翻訳ファイル作成（英語） (2h)
5. [ ] ルート構造変更 `[locale]` (2h)
6. [ ] LanguageSwitcher コンポーネント (45min)
7. [ ] middleware.ts 更新 (30min)
8. [ ] 各ページの翻訳適用 (2h)
9. [ ] テスト (1h)

**依存パッケージ**:
```bash
pnpm add next-intl --filter web
```

---

### 6. ブログ機能

**目的**: Qiita以外の独自コンテンツで専門性をアピール

#### 要件定義

| 項目 | 内容 |
|------|------|
| コンテンツ形式 | MDX |
| 機能 | カテゴリ、タグ、検索、目次 |
| URL | `/blog`, `/blog/[slug]` |

#### 実装計画

**ファイル構造**:
```
apps/web/
├── content/
│   └── blog/
│       ├── first-post.mdx
│       └── second-post.mdx
├── lib/
│   └── mdx/
│       ├── get-posts.ts      # 記事取得
│       ├── mdx-components.tsx # MDXコンポーネント
│       └── types.ts
├── app/
│   └── blog/
│       ├── page.tsx          # 記事一覧
│       └── [slug]/
│           └── page.tsx      # 記事詳細
├── components/
│   └── features/
│       └── Blog/
│           ├── BlogCard.tsx
│           ├── BlogList.tsx
│           ├── TableOfContents.tsx
│           ├── CodeBlock.tsx
│           └── index.ts
```

**MDX記事例**:
```mdx
---
title: "Next.js 15の新機能まとめ"
description: "Next.js 15で追加された機能を詳しく解説します"
publishedAt: "2026-01-15"
category: "技術記事"
tags: ["Next.js", "React", "TypeScript"]
---

# Next.js 15の新機能まとめ

## はじめに

Next.js 15がリリースされました...

<CodeBlock language="typescript">
// コード例
const greeting = "Hello, World!";
</CodeBlock>
```

**実装ステップ**:
1. [ ] MDX関連パッケージインストール (15min)
2. [ ] MDX設定（next.config.js） (30min)
3. [ ] 記事取得ユーティリティ (1h)
4. [ ] MDXコンポーネント（CodeBlock等） (2h)
5. [ ] ブログ一覧ページ (1.5h)
6. [ ] ブログ詳細ページ (2h)
7. [ ] TableOfContents (1h)
8. [ ] 検索機能（オプション） (2h)
9. [ ] サンプル記事作成 (1h)
10. [ ] テスト (1h)

**依存パッケージ**:
```bash
pnpm add @next/mdx @mdx-js/loader @mdx-js/react gray-matter reading-time --filter web
pnpm add -D @types/mdx --filter web
```

---

### 7. お問い合わせ履歴永続化

**目的**: お問い合わせ内容を管理・追跡

#### 要件定義

| 項目 | 内容 |
|------|------|
| データベース | Supabase（推奨）or PlanetScale |
| 認証 | Supabase Auth |
| 管理画面 | `/admin` (認証必須) |

#### 実装計画

**ファイル構造**:
```
apps/api/
├── src/
│   └── modules/
│       └── contact/
│           ├── contact.entity.ts     # DBエンティティ
│           ├── contact.repository.ts # リポジトリ
│           └── contact.service.ts    # 保存ロジック追加

apps/web/
├── app/
│   └── admin/
│       ├── layout.tsx        # 認証チェック
│       ├── page.tsx          # ダッシュボード
│       └── contacts/
│           └── page.tsx      # お問い合わせ一覧
├── lib/
│   └── supabase/
│       ├── client.ts         # クライアント
│       └── server.ts         # サーバー用
```

**データベーススキーマ**:
```sql
CREATE TABLE contacts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  email VARCHAR(255) NOT NULL,
  message TEXT NOT NULL,
  status VARCHAR(20) DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

**実装ステップ**:
1. [ ] Supabase プロジェクト作成 (30min)
2. [ ] テーブル作成 (15min)
3. [ ] Supabase クライアント設定 (30min)
4. [ ] Contact エンティティ/リポジトリ (1h)
5. [ ] ContactService 更新（保存追加） (45min)
6. [ ] 認証設定 (1h)
7. [ ] 管理画面レイアウト (1h)
8. [ ] お問い合わせ一覧ページ (2h)
9. [ ] ステータス更新機能 (1h)
10. [ ] テスト (1.5h)

**依存パッケージ**:
```bash
# Backend
pnpm add @supabase/supabase-js --filter api

# Frontend
pnpm add @supabase/ssr @supabase/supabase-js --filter web
```

---

## 技術的考慮事項

### パフォーマンス

| 項目 | 対策 |
|------|------|
| バンドルサイズ | framer-motionは動的インポート |
| 画像最適化 | next/image使用 |
| フォント | next/font最適化済み |
| キャッシュ | ISR活用（revalidate: 600） |

### アクセシビリティ

| 項目 | 対策 |
|------|------|
| キーボード操作 | 全インタラクティブ要素対応 |
| スクリーンリーダー | aria-label, role適切に |
| カラーコントラスト | WCAG AA準拠 |
| フォーカス管理 | visible focus ring |

### SEO

| 項目 | 対策 |
|------|------|
| メタデータ | 各ページに個別設定 |
| 構造化データ | JSON-LD継続 |
| sitemap | 動的生成対応 |
| OGP | 各ページに設定 |

---

## リスクと対策

| リスク | 影響 | 対策 |
|--------|:----:|------|
| framer-motionバンドルサイズ増加 | 中 | 動的インポート、tree shaking |
| i18n導入による複雑化 | 中 | 段階的導入、自動テスト強化 |
| MDXビルド時間増加 | 低 | 記事数制限、増加時はCMS検討 |
| Supabase依存 | 中 | 抽象化レイヤー、バックアップ |

---

## 見積りサマリ

| Phase | 機能 | 工数 |
|-------|------|:----:|
| Phase 1 | Analytics + アニメーション | 3-4h |
| Phase 2 | スキル + プロジェクト詳細 | 12-18h |
| Phase 3 | i18n + ブログ | 24-36h |
| Phase 4 | DB永続化 | 12-20h |
| **合計** | | **51-78h** |

---

## 次のステップ

1. **即座に開始可能**: Phase 1（Analytics、アニメーション）
2. **優先実装推奨**: スキルセクション（採用訴求に直結）
3. **検討事項**: ブログvsQiitaの使い分け方針

実装を開始する機能があれば、お知らせください。
